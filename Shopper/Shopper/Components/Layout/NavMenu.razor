<div class="navbar-wrapper">
    <div class="top-row navbar navbar-dark">
        <div class="left-side">
            <select>
                <option value="main">Lista główna</option>
                <option value="second">Lista dodatkowa</option>
                <option value="shared">Real time sync</option>
            </select>
        </div>

        <label for="hamburger" class="hamburger-label">
            <span></span>
            <span></span>
            <span></span>
        </label>
    </div>
</div>

<input type="checkbox" id="hamburger" class="hamburger-checkbox" />

<div class="menu-overlay" onclick="document.getElementById('hamburger').checked = false"></div>

<div class="nav-scrollable">
    <nav class="flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="shopping-list" onclick="document.getElementById('hamburger').checked = false">
                <span class="bi bi-list-nested-nav-menu"></span> Shopping list
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="edit-shopping-list" onclick="document.getElementById('hamburger').checked = false">
                <span class="bi bi-list-nested-nav-menu"></span> Edit shopping list
            </NavLink>
        </div>

    </nav>
</div>

<style>

    .navbar-wrapper {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 3000;
    }

    .top-row.navbar {
        background: var(--panel-rich-color);
        border-bottom: 1px solid rgba(var(--accent-rgb), 0.08);
        padding: 0.6rem 1rem;
        height: 56px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .left-side {
        display: flex;
        align-items: center;
    }

    .hamburger-label {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 5px;
        padding: 6px 10px;
        cursor: pointer;
        z-index: 3100;
    }

        .hamburger-label span {
            width: 28px;
            height: 3px;
            background: var(--neon);
            border-radius: 3px;
        }

    .hamburger-checkbox {
        display: none;
    }

    .menu-overlay {
        position: fixed;
        top: 56px;
        left: 0;
        right: 0;
        bottom: 0;
        background: transparent;
        z-index: 1500;
        display: none;
    }

    #hamburger:checked ~ .menu-overlay {
        display: block;
    }

    .nav-scrollable {
        position: fixed;
        top: 56px;
        left: 0;
        right: 0;
        background: var(--panel-rich-color);
        border-bottom: 1px solid rgba(var(--accent-rgb), 0.05);
        transform: translateY(-100%);
        opacity: 0;
        transition: transform 0.25s ease, opacity 0.25s ease;
        z-index: 2000;
    }

    #hamburger:checked ~ .nav-scrollable {
        transform: translateY(0);
        opacity: 1;
    }

    .nav-scrollable nav {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
    }

    .nav-item {
        margin: 0.25rem 0;
        border-bottom: 0.5px solid var(--neon);
    }

    .nav-link {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        color: var(--starlight);
        padding: 0.75rem 1rem;
        text-decoration: none;
        border-radius: 10px;
        font-size: 0.9rem;
        transition: background-color 0.2s ease, color 0.2s ease, box-shadow 0.2s ease;
    }

        .nav-link.active {
            background: rgba(var(--accent-rgb), 0.12); 
            color: var(--neon);
            box-shadow: inset 0 0 4px rgba(var(--accent-rgb), 0.25); 
        }

        .nav-link:hover {
            background-color: rgba(var(--accent-rgb), 0.18);
            color: var(--neon);
            box-shadow: inset 0 0 6px rgba(var(--accent-rgb), 0.28);
        }

        .nav-link .bi {
            color: var(--starlight);
            transition: color 0.2s ease;
        }

        .nav-link:hover .bi,
        .nav-link.active .bi {
            color: var(--neon);
        }

</style>
